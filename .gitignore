node_modules
dist
.DS_Store
*.local
.vercel
.superpowers/

# Secrets — NEVER commit
.env
.env.local
.env.*.local

# macOS Finder duplicates (files like "index 2.html")
* 2.*

# External design reference clone — not part of this project
awesome-design-md/
